{
    "name": "vtf-audio-extension",
    "version": "0.5.0",
    "description": "VTF Audio Transcription Chrome Extension",
    "private": true,
    "scripts": {
        "test": "node scripts/test.js",
        "test:unit": "node scripts/test.js unit",
        "test:integration": "node scripts/test.js integration",
        "build": "node scripts/build.js",
        "build:prod": "NODE_ENV=production node scripts/build.js",
        "build:legacy": "node scripts/build.js --legacy",
        "watch": "node scripts/build.js --watch",
        "lint": "eslint src/",
        "migrate": "node scripts/migrate-settings.js"
    },
    "devDependencies": {
        "chrome-types": "^0.1.0",
        "esbuild": "^0.25.5",
        "eslint": "^8.0.0"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}
