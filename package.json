{
    "name": "vtf-audio-extension",
    "version": "0.5.0",
    "description": "VTF Audio Transcription Chrome Extension",
    "private": true,
    "scripts": {
        "clean": "rm -rf dist",
        "copy:static": "mkdir -p dist/inject dist/icons dist/workers && cp src/inject/inject.js dist/inject/ && cp -r src/icons/* dist/icons/ && cp src/manifest.json dist/ && cp src/workers/audio-worklet.js dist/workers/",
        "build:content": "esbuild src/content.js --bundle --outfile=dist/content.js --format=iife",
        "build:background": "esbuild src/background.js --bundle --outfile=dist/background.js --format=iife",
        "build": "npm run clean && npm run copy:static && npm run build:content && npm run build:background"
    },
    "devDependencies": {
        "chalk": "^5.3.0",
        "chokidar": "^3.5.3",
        "esbuild": "^0.25.5"
    },
    "author": "Your Name",
    "license": "MIT",
    "engines": {
        "node": ">=18.0.0",
        "npm": ">=8.0.0"
    }
}
